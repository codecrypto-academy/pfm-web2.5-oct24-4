export interface Network {
  networkName: string;
  chainId: string;
  subnet: string;
  ipBootNode: string;
}

export interface NetworkDetailsData {
  networkName: string;
  chainId: string;
  subnet: string;
  ipBootNode: string;
}
